language: java
branches:
  only:
    - master
    
 script:
  mvn clean install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
  

deploy:
  provider: cloudfoundry
  edge: true
  api: $CF_API
  username: $CF_USER
  password: $CF_PASS
  organization: $CF_ORG
  space: $CF_ENV 